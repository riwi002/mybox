# 2026-06-07 工作日志

## 主菜单新增 ngxing管理器（选项14）
- 在 riwi_sh() 主菜单添加第14项"ngxing管理器"
- 实现 ngxing_manager() 函数，包含 Nginx 状态/启动/停止/重启/配置/测试/日志/重载 8个子功能

## Docker管理子菜单新增关闭/重启选项
- 菜单显示：选项10"关闭Docker服务"，选项13"重启Docker服务"
- case 处理代码：10 用 systemctl stop docker，13 用 systemctl restart docker

## 全局菜单颜色批量替换
- 用 Python 三阶段替换所有菜单/子菜单 echo 行：
  - 数字序列号（N.）改为黄色 ${rw_huang}
  - 中文/描述文字改为绿色 ${rw_lv}
  - 分隔线、框线（╔╚║---）保持原青色 ${riwi001} 不变
- 共修改约 215 行，涵盖主菜单 + 全部子菜单
- 修复了双列菜单行第二列颜色
- 清理了重复的 ${rw_bai} 问题
- 备份文件：riwi.sh.bak_color

## 全局菜单序号对齐
- 用 Python 正则替换所有 ${rw_huang}N.空格+${rw_bai} 格式，统一对齐文字起始列
- 规则：1位数→点后3空格，2位数→点后2空格，3位数→点后1空格（总宽均为5字符）
- 共修改 106 行，涵盖主菜单和所有子菜单（含LDNMP、Docker、安全、工具等）
- 备份文件：riwi.sh.bak_align
