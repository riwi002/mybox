# 2026-07-12 工作日志

## Git选项8「回退到历史版本」优化修复
- **Bug修复**：原代码 `git reset` 后只回退本地 HEAD，不推送远程 → 用户看到"成功"但远程没变
- 修复：回退成功后新增提示流程，让用户选择是否 `git push --force-with-lease` 到远程
- **提示优化**：
  - 提交列表下方增加提示"左侧序号仅显示，请输入右侧哈希值"
  - 输入提交ID后验证有效性，无效则报错退出
  - 显示目标提交详细信息（ID/说明/时间/作者）确认框
  - 软回退/硬回退说明改为多行详细解释，含适用场景
  - 硬回退前列出将被丢弃的提交和总数
  - 回退后明确提示"本地已回退，远程未变"并引导推送选择
- 使用 `--force-with-lease` 而非 `--force`，更安全
- 备份: riwi.sh.bak_rollback_fix_20260712

## 修复回退后推送 detached HEAD 的 Bug
- **问题**：`git reset --hard` 后在某些仓库会进入 detached HEAD 状态，`git rev-parse --abbrev-ref HEAD` 返回 `"HEAD"` 而非分支名
- 导致 `git push --force-with-lease origin HEAD` 报错 "not a full refname"
- **修复**：检测到 `_cur_branch == "HEAD"` 时，先尝试从 `@{u}` 远程跟踪分支推断，再不行就列出本地分支让用户手动输入
- 备份: riwi.sh.bak_rollback_push_fix_20260712

## 确认选项15「强制推送」已删除
- 选项15在之前的操作中已从菜单和 case 分支中完全移除
- 当前 git 管理菜单只到选项14（GitHooks 部署）
