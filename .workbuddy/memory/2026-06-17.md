# 2026-06-17 工作日志

## 新增用户管理器模块

- 在 `riwi_sh()` 主菜单（约22352行）添加了第 12 项「用户管理器」
- 在 case 中绑定 `12) user_manager ;;`
- 新增三个函数（插入在 riwi_sh() 结束后、Nginx管理器之前，约22388行）：
  - `root_manager()`：先添加了7项（SSH/密码/密钥/锁定等），后追加了密钥登录交互（粘贴/GitHub/本地文件），最后按用户要求全部删掉重写为简洁4步流程：创建普通用户 → 设密码 → 问是否给sudo → 显示当前sudo用户列表
  - `new_user_manager()`：先添加了7项（创建/删除/密码/列表/sudo/密钥权限管理），最后改为直接复用 root_manager()
  - `user_manager()`：入口菜单，包含1.root管理 / 2.新用户管理 两个子菜单
- 密钥权限管理子菜单曾包含：添加SSH公钥（粘贴/GitHub/本地文件）、查看密钥、清空密钥、修复.ssh=700/authorized_keys=600权限
