## 2026-06-06 工作日志

### GitHub 管理器 - 推送到远程超时修复
- 问题：`git push` 在 macOS 上网络不通时会卡住，因为没有 `timeout` 命令
- 第一次修复：添加了 `run_with_timeout` 函数，使用 `pkill -P` 杀子进程 + `kill` 杀主进程
- 问题未解决：`pkill -P` 无法正确终止 `git-remote-https` 子进程，`wait` 仍然卡住
- 最终修复：使用 `set -m` 启用作业控制创建独立进程组，`kill -TERM -$pid` 向整个进程组发信号，确保主进程和所有子进程同时被终止
- 调用位置：推送到远程(22977)、拉取并重新推送(23001/23004)、变基并重新推送(23016/23019)、强制推送(23036)

### 日常维护菜单扩充
- 将系统工具菜单中的14个功能迁移到 `update_clean_menu`（日常维护）
- 迁移的条目：系统时区、修改主机名、重启服务器、一键重装系统、切换IPv4/IPv6、定时任务、系统内核优化、系统备份恢复、SSH密钥登录、修复SSH漏洞、系统回收站、系统变量管理、TG-bot监控预警、隐私与安全
- 按功能分组排序（系统基础 → 网络/任务 → 安全 → 监控/隐私），名称精简为2-4个字

