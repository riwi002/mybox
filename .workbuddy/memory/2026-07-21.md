# 2026-07-21 工作日志

## 3X-UI 下载脚本失败修复
- 问题：install_3xui 中 curl 下载官方 install.sh 时直接用 `raw.githubusercontent.com`，未使用全局 `gh_proxy` 变量，国内服务器被墙导致下载失败
- 修复：改用 `${gh_proxy}${_raw_path}` 下载，三层回退：
  1. gh_proxy 代理（国内为 `https://gh.riwi.pro/`）
  2. 直连 `https://raw.githubusercontent.com/...`
  3. 备用代理 `https://ghproxy.net/...`
- 失败时提示手动下载 URL
- bash -n 语法检查通过
- 已 git push (commit aeb2dd7)

## 3X-UI install_3xui 函数完全重写
- Owen 要求：按新规格重写，不再用 sed 翻译层，改用环境变量原生汉化
- 删除 `translate_3xui_script()` 函数（~260行 sed 翻译规则，已废弃）
- 新增 `check_zh_locale()` 函数：
  - 检查 LANG/LC_ALL 是否包含 zh_CN 或 UTF-8
  - 检查 `locale -a` 是否有 zh_CN.UTF-8
  - 交互式安装中文语言包：apt→language-pack-zh-hans / dnf→glibc-langpack-zh / yum→kde-l10n-Chinese
  - 安装后自动 locale-gen zh_CN.UTF-8
- install_3xui 核心变化：
  - 执行前 `export LANG=zh_CN.UTF-8` + `export LC_ALL=zh_CN.UTF-8`，让官方脚本原生输出中文
  - 安装方式改回 `bash <(curl -Ls URL)` 三层回退（gh_proxy → 直连 → ghproxy.net）
  - 不再下载临时文件、不再做 sed 翻译
  - 菜单项文案：1.安装稳定版本 2.安装特定版本 3.安装开发版本
  - 安装前提示"正在调用官方脚本安装 3X-UI 面板..."
  - 安装后提示"3X-UI 安装已结束"
- 保留：基础呼出子菜单、架构检查、已安装检测、curl 检测
- bash -n 语法检查通过
- 已 git push (commit 12e1030, -304/+96 行)
