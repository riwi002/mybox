# 2026-07-21 工作日志

## 3X-UI 下载脚本失败修复
- 问题：install_3xui 中 curl 下载官方 install.sh 时直接用 `raw.githubusercontent.com`，未使用全局 `gh_proxy` 变量，国内服务器被墙导致下载失败
- 修复：改用 `${gh_proxy}${_raw_path}` 下载，三层回退：
  1. gh_proxy 代理（国内为 `https://gh.riwi.pro/`）
  2. 直连 `https://raw.githubusercontent.com/...`
  3. 备用代理 `https://ghproxy.net/...`
- 失败时提示手动下载 URL
- bash -n 语法检查通过
- 已 git push (commit aeb2dd7)

## 3X-UI install_3xui 函数完全重写
- Owen 要求：按新规格重写，不再用 sed 翻译层，改用环境变量原生汉化
- 删除 `translate_3xui_script()` 函数（~260行 sed 翻译规则，已废弃）
- 新增 `check_zh_locale()` 函数：
  - 检查 LANG/LC_ALL 是否包含 zh_CN 或 UTF-8
  - 检查 `locale -a` 是否有 zh_CN.UTF-8
  - 交互式安装中文语言包：apt→language-pack-zh-hans / dnf→glibc-langpack-zh / yum→kde-l10n-Chinese
  - 安装后自动 locale-gen zh_CN.UTF-8
- install_3xui 核心变化：
  - 执行前 `export LANG=zh_CN.UTF-8` + `export LC_ALL=zh_CN.UTF-8`，让官方脚本原生输出中文
  - 安装方式改回 `bash <(curl -Ls URL)` 三层回退（gh_proxy → 直连 → ghproxy.net）
  - 不再下载临时文件、不再做 sed 翻译
  - 菜单项文案：1.安装稳定版本 2.安装特定版本 3.安装开发版本
  - 安装前提示"正在调用官方脚本安装 3X-UI 面板..."
  - 安装后提示"3X-UI 安装已结束"
- 保留：基础呼出子菜单、架构检查、已安装检测、curl 检测
- bash -n 语法检查通过
- 已 git push (commit 12e1030, -304/+96 行)

## 删除 3X-UI 选项及相关代码
- Owen 要求：从应用市场完全删除 3X-UI 选项
- 删除内容：
  1. `check_zh_locale()` 函数定义（~68行）
  2. `install_3xui()` 函数定义（~223行）
  3. 菜单中 "2. 3X-UI 多协议代理面板" 行
  4. case 块中选项 2 的 `install_3xui` 调用
  5. `install_xray_lite` 中"与 3X-UI 的区别"文字改为"特点"
- 菜单重新编号：1.1Panel / 2.xray迷你版 / 3.虚拟机Quickemu
- grep 验证：脚本中无任何 3x-ui/3X-UI/3xui/install_3xui/check_zh_locale 残留
- bash -n 语法检查通过
- 已 git push (commit 65147d6, -312/+5 行)

## 重新添加 install_3xui 函数（精简版）
- Owen 要求：按新规格重新编写并添加到应用市场
- 新函数特点（比之前精简，无 check_zh_locale / 无基础呼出子菜单 / 无架构检查）：
  1. 交互式安装向导菜单：1.稳定版 2.指定版本 3.开发版 0.返回
  2. `bash <(curl -Ls URL)` 方式执行，三层代理回退（gh_proxy → 直连 → ghproxy.net）
  3. 选项2指定版本：空输入时提示重新输入或 q 取消；自动补 v 前缀
  4. curl 检测：不可用时自动 install curl，失败则报错退出
  5. URL 用 local 变量定义（_3xui_url / _3xui_url_direct / _3xui_url_backup）
  6. 安装后提示"安装流程已结束，请查看上方输出以确认状态" + 常用管理命令
- 菜单：1.1Panel / 2.xray迷你版 / 3.虚拟机Quickemu / 4.3X-UI多协议代理面板
- case 块选项 4 调用 install_3xui
- 函数位置: L17025（install_xray_lite 前）
- bash -n 语法检查通过
- 已 git push (commit ef99089, +157 行)

## install_3xui 交互体验增强
- Owen 要求：每个操作后加暂停，网络失败给友好提示
- 改动点：
  1. 每个安装选项执行后，如果 _rc≠0，显示红色网络错误提示（检查 GitHub 连通性/版本号是否存在）
  2. curl 安装失败时显示手动安装命令（yum/apt）
  3. 安装失败时也显示常用管理命令（可能已有旧版）
  4. 结果反馈区域末尾加 `read -n 1 -s -r -p "按任意键继续..."` 暂停
- bash -n 语法检查通过
- 已 git push (commit 8310694, +48/-19 行)

## install_3xui 按 install_xray_lite 风格重写（最终版）
- Owen 要求：按 xray 迷你版的代码逻辑重新编写 3X-UI
- 核心变化（与 xray_lite 结构完全对齐）：
  1. 去掉安装向导子菜单（稳定版/指定版本/开发版三选一）→ 改为直接 `确认开始安装？(y/N)`
  2. 去掉三层代理回退 → 改为直接 `bash <(curl -Ls URL)` 单一执行
  3. 去掉末尾"按任意键继续"暂停（由调用处 break_end 统一处理）
  4. 新增系统架构检查（amd64/arm64/armv7/armv6/armv5/386/s390x）
  5. 新增已安装检测（x-ui 命令或 systemd 服务）→ 重新安装确认
  6. 执行前打印命令行 `命令: bash <(curl -Ls ...)`
  7. 安装结果分成功/失败两支：成功显示管理命令列表；失败显示常见原因
- 函数体量：~112行（与 xray_lite ~136行相当）
- bash -n 语法检查通过
- 本地 commit b4d8fb5（+81/-147 行），GitHub 502 待 push
