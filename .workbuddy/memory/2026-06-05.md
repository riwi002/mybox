# 2026-06-05 工作日志

## 任务：riwi.sh 菜单和输出优化

### 已完成的工作

1. **英文 echo 输出翻译为中文**
   - 批量替换了脚本中所有用户可见的英文提示为中文
   - GitHub 管理器部分保持原样未改动
   - 保留了技术术语（URL、配置键名、命令参数等）
   - 将 "返回上一级选单" 统一改为 "返回上一级菜单"（55处）

2. **菜单导航改进：输入0返回上一级，回车默认0返回**
   - 所有63个子菜单的提示语已添加 "（回车默认0返回）"
   - 所有子菜单已添加 `变量=${变量:-0}` 默认值设置（55处新增）
   - 修复了7处重复的提示语（如 "（回车默认0返回）（回车默认0返回）"）
   - 主菜单的0保持为"退出脚本"

3. **添加实例操作功能**
   - 在主菜单添加了第13项 "实例操作"
   - 新增 `instance_manager()` 函数，包含以下功能：
     - 查看运行中的服务
     - 启动/停止/重启服务
     - 查看活跃进程
     - 查看Docker容器实例
     - 查看网站实例
   - 实例操作菜单同样支持输入0返回、回车默认0返回

4. **语法验证**
   - 使用 `bash -n` 检查脚本语法，确认无错误
