# 2026-06-04 工作日志

## riwi.sh 主菜单重构

- 将主菜单原 2（系统更新）和 3（系统清理）合并为一个入口：**2. 更新清理**
- 后续选项依次前移：原 4→3、5→4、6→5、7→6、8→7、9→8、10→9、11→10、12→11、13→12
- 新增子菜单函数 `update_clean_menu()`（位于第 21482 行），包含：
  - 1. 系统更新 → `linux_update()`
  - 2. 系统清理 → `linux_clean()`
  - 0. 返回主菜单
- `riwi_sh()` 主菜单中选项 2 改为调用 `update_clean_menu`

## 简单LDNMP管理器优化（第9238行）

- 翻译 `certs_status()` 中的英文提示：
  - `Let's Encrypt` → 保留品牌名，加注中文说明
  - `CRT/PEM` → `CRT/PEM 格式`
  - `Private Key` → `私钥`
  - `ECC` → `ECC（椭圆曲线）`
  - `RSA` → `RSA`
- 优化主菜单布局，新增3个功能入口：
  - 9. SSL证书管理 → `ldnmp_ssl_manage()`（查看证书、申请/续签、删除证书）
  - 10. 备份/恢复 → `ldnmp_backup_restore()`（备份站点数据、恢复备份）
  - 11. 查看日志 → `ldnmp_view_logs()`（Nginx/PHP/MySQL/Redis日志）
  - 12. 卸载LDNMP环境（原9号菜单，移至12）
- 新增三个函数插入位置：第9446行（`simple_ldnmp_manager` 结束之后，`moltbot_menu` 之前）
- 语法检查通过（`bash -n` 无报错）

## 删除简单LDNMP管理器（第9238行）

- 应要求删除主菜单选项 6（简单LDNMP管理器）及其所有相关代码
- 删除内容：
  - `simple_ldnmp_manager()` 函数（原第9238-9441行，共204行）
  - `ldnmp_ssl_manage()` 函数（原第9446-约9500行）
  - `ldnmp_backup_restore()` 函数（原第约9501-约9580行）
  - `ldnmp_view_logs()` 函数（原第约9581-9647行）
  - 共计删除 406 行代码
- 主菜单调整：
  - 删除选项 6（简单LDNMP管理器）
  - 原选项 7-12 前移至 6-11
  - 新菜单结构：1.系统查询 2.更新清理 3.基础工具 4.GitHub管理器 5.Docker管理 6.应用市场 7.后台工作区 8.系统工具 9.服务器集群控制 10.安全优化 11.广告专栏
- 语法检查通过（`bash -n` 无报错）

## 主菜单添加LDNMP建站（第9242行）

- 应要求在主菜单添加"LDNMP建站"入口
- 主菜单调整：
  - 原选项6（应用市场）→ 7
  - 原选项7（后台工作区）→ 8
  - 原选项8（系统工具）→ 9
  - 原选项9（服务器集群控制）→ 10
  - 原选项10（安全优化）→ 11
  - 原选项11（广告专栏）→ 12
  - 新增选项6（LDNMP建站）
- 新增 `ldnmp_builder_menu()` 函数（第9242行），包含14个功能：
  1. 安装LDNMP环境 → `ldnmp_install_env()`
  2. 安装Typecho轻量博客网站 → `ldnmp_install_typecho()`
  3. 仅安装nginx → `ldnmp_install_nginx_only()`
  4. 站点反向代理+IP+端口 → `ldnmp_proxy_ip_port()`
  5. 站点数据管理 → `ldnmp_site_data_manage()`
  6. 定时远程备份 → `ldnmp_scheduled_backup()`
  7. 防护LDNMP环境 → `ldnmp_protect_env()`
  8. 更新LDNMP环境 → `ldnmp_update_env()`
  9. 优化LDNMP环境 → `ldnmp_optimize_env()`
  10. 还原全站数据 → `ldnmp_restore_full()`
  11. 自定义静态站点 → `ldnmp_custom_static()`
  12. 站点反向代理-负载均衡 → `ldnmp_proxy_load_balance()`
  13. 站点反向代理+域名 → `ldnmp_proxy_domain()`
  14. 站点重定向 → `ldnmp_site_redirect()`
- 所有14个功能函数已替换为真实实现（原桩函数显示"功能开发中"）
- 语法检查通过（`bash -n` 无报错）

