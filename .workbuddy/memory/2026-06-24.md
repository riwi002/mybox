# 2026-06-24 工作日志

## riwi.sh 密钥管理菜单重写（双证/三证/硬证登录）

用户反馈主菜单 12.密钥管理 里的 2/3/4（双证/三证/硬证登录）功能与 sshd_config 设置混乱，要求重写交互和执行逻辑。

### 问题诊断
1. 菜单文案与绑定函数对不上：双证绑定 ssh_config_manager（其实是通用 SSH 配置），三证绑定 ssh_mfa_manager（实际是公钥+OTP 双因素），命名混乱。
2. sshd_config 互相打架：双证写 PasswordAuthentication no，三证写 AuthenticationMethods 但不检查前者，硬证只管 PubkeyAuthentication，三者互相覆盖、不清理冲突。
3. 交互流程不统一：备份/改配置/sshd -t/重启逻辑三处各写一遍，写法不一，有的不回滚。

### 用户确认的方案
- 凭证定义：双证=公钥+OTP（不要密码），三证=公钥+密码+OTP
- 冲突处理：切换时自动备份 + 清理冲突项 + 支持回滚

### 完成的改动（riwi.sh 23143-24394 行替换，原 1252 行 → 新代码块）
- 删除旧函数：ssh_mfa_manager
- 新增 12 个函数：
  - 共用辅助：_ssh_cfg_get / _ssh_current_auth_mode / _ssh_status_panel / _ssh_backup_configs / _ssh_clear_conflicting_auth / _ssh_cfg_set / _ssh_apply_and_restart / _ssh_restore_backup / _ssh_otp_configure_user
  - ssh_two_factor_manager（双证=公钥+OTP）：AuthenticationMethods publickey,keyboard-interactive + PAM + PasswordAuth no
  - ssh_three_factor_manager（三证=公钥+密码+OTP）：AuthenticationMethods publickey,password,keyboard-interactive + PAM + PasswordAuth yes
  - ssh_fido2_manager（硬证=FIDO2）：重构分区（服务端配置/客户端密钥生成/部署测试/卸载），切换前调用 _ssh_clear_conflicting_auth
- user_manager 菜单文案更新为「双证登录（公钥 + OTP 动态码）」「三证登录（公钥 + 密码 + OTP 动态码）」「硬证登录（FIDO2 硬件密钥）」
- ssh_config_manager 保留（它是通用 SSH 配置，不在 user_manager 菜单里，是独立功能）

### 验证
- bash -n 语法检查通过
- 12 个新函数定义位置正确，user_manager 菜单 case 分支指向新函数
- ssh_fido2_manager 结束后正确接 ngxing_manager

### 备份
- riwi.sh.bak_20260624_auth（重写前备份）

### 关键设计点
- _ssh_current_auth_mode 探测当前实际生效模式（2fa/3fa/fido2/pubkey-only/password-only 等），三个菜单顶部共用 _ssh_status_panel 显示
- _ssh_clear_conflicting_auth 在切换模式前清理：注释 AuthenticationMethods、删当前用户的 Match User 块、移除 PAM google_authenticator 行
- _ssh_apply_and_restart 统一 sshd -t 检查 + 失败自动回滚 + 询问重启
- _ssh_backup_configs 备份到 /tmp/riwi_ssh_backup_时间戳/（sshd_config + pam.d/sshd + authorized_keys）

## riwi.sh 应用市场重写（linux_panel）

用户要求清空主菜单 5.应用市场的全部内容，重新写，先加 1Panel。

### 用户确认的方案
- 1Panel 安装方式：官方一键脚本（resource.fit2cloud.com/1panel/package/install.sh）
- 菜单范围：只装不用（安装完显示访问信息即可）

### 完成的改动（riwi.sh 15775-19653 行替换，原 3879 行 → 新 169 行）
- 删除旧的 linux_panel：原本依赖远程 git 仓库 github.com/riwi/apps.git 拉取应用配置再 source 执行，列表含宝塔/aaPanel/1Panel/NginxProxyManager/Cloudreve/青龙/portainer 等 30+ 应用，机制脆弱
- 新 linux_panel：干净菜单 + 1Panel 单入口
  - 顶部状态说明（特性/依赖/官网/开源地址）
  - 执行前确认 + 系统架构检查（x86_64/arm64）
  - 已安装检测（1pctl 命令 / 1panel 服务）+ 询问是否重装
  - 下载官方 install.sh 到 /tmp 再执行（curl 优先，wget 回退，都没有则 install curl）
  - 安装完成后打印常用管理命令（1pctl status/user-info/restart/stop/version/update）
  - 失败时打印排查提示
- 主菜单 5) linux_panel 调用不变，CLI 入口 linux_panel "$@" 兼容（新版第一行接 $1）

### 验证
- bash -n 语法检查通过
- 旧应用列表（宝塔/aaPanel/青龙/Cloudreve/portainer）引用归零
- riwi/apps.git 引用归零
- linux_panel 结束后正确接 linux_work

### 备份
- riwi.sh.bak_20260624_appmarket（重写前备份）

### 文件行数变化
- 重写前：29150 行
- 重写后：25440 行（减少 3710 行）
