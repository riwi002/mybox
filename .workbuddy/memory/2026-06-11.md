# 2026-06-11 工作日志

## Docker 全管理 — 新增 Colima 管理

### 操作内容
- 在 `docker_manager_menu()` 函数中新增 Colima 状态探测和4个管理选项（11-14）
- 布局按照现有菜单风格，新增"Colima 管理"分组

### Colima 管理子菜单

| 编号 | 功能 | colima 命令 |
|------|------|------------|
| 11 | 查看运行状态 | `colima status` + `colima list` |
| 12 | 停止容器环境 | `colima stop` |
| 13 | 重启服务 | `colima restart` |
| 14 | 删除实例 | `colima delete`（需输入 YES 确认）|

### 状态栏
- 自动探测 colima 是否已安装
- 已安装时显示"运行中"或"已安装未运行"

### 未安装时的提示
- 显示提示：macOS 用户可使用 colima 替代 Docker Desktop
- 显示安装命令：`brew install colima docker docker-compose`

### 语法验证
`bash -n` ✅ 通过

---

## 安装环境 — 优化输入提示实例

### 操作内容
- 优化选项33（安装指定工具）的输入提示
- 优化选项34（卸载指定工具）的输入提示

### 优化前 vs 优化后

| | 优化前 | 优化后 |
|---|---|---|
| 选项33提示 | `请输入安装的工具名（wget curl sudo htop）:` | 带标题行、示例（工具名/编号两种格式）|
| 选项34提示 | `请输入卸载的工具名（htop ufw tmux cmatrix）:` | 同上 |
| 空输入处理 | 无 | 提示"已取消"并 continue |

### 示例输出
```
──── 安装指定工具 ────
 提示: 多个工具用空格分隔，支持工具名或编号
 示例:
   工具名: wget curl htop vim
   编号:   1 2 5 15

 请输入要安装的工具:
```

### 语法验证
`bash -n` ✅ 通过

---

## Docker 全管理 — 优化加载速度

### 问题
`docker_manager_menu()` 每次打开菜单都执行 `docker ps -q`、`docker ps -aq`、`docker images -q`，
当 Docker 未运行或未响应时，这些命令会卡住 10-30 秒等待超时。

### 优化方案

#### 1. 新增缓存变量（第540-543行）
```bash
_CACHE_COLIMA_ACTIVE=false
_CACHE_DOCKER_CONTAINER_RUNNING=0
_CACHE_DOCKER_CONTAINER_ALL=0
_CACHE_DOCKER_IMAGE_COUNT=0
```

#### 2. 修改 `refresh_status_cache()` 函数（第612-621行）
- Docker 活跃时，用 `timeout 5` 执行 `docker ps` 等命令
- 将容器数/镜像数存入缓存变量
- 添加 Colima 状态缓存（`timeout 5 colima status`）

#### 3. 修改 `docker_manager_menu()` 函数（第24518-24532行）
- **使用缓存值**：`_dr=$_CACHE_DOCKER_CONTAINER_RUNNING` 等
- **未运行时不执行 docker 命令**：避免卡住
- **Colima 状态使用缓存**：`_CACHE_COLIMA_ACTIVE`

### 优化效果
| 场景 | 优化前 | 优化后 |
|------|--------|--------|
| Docker 运行中 | 每次执行3条 docker 命令 | 使用缓存（60秒有效） |
| Docker 未运行 | 卡住 10-30 秒 | 立即返回，不执行 docker 命令 |
| Colima 已安装 | 每次执行 `colima status` | 使用缓存 |

### 语法验证
`bash -n` ✅ 通过

---

## 许可协议管道模式修复

### 问题
通过 `curl -sL URL | bash` 管道运行时，stdin 被管道占用，`read` 无法读取键盘输入，`_license_agree` 为空，直接走到"未同意"退出。

### 修复
将 `read -e -p "是否同意以上条款？(y/n): " _license_agree` 改为：
```bash
read -e -p "是否同意以上条款？(y/n): " _license_agree < /dev/tty
```
`/dev/tty` 始终指向当前终端，不受管道影响。

### 语法验证
`bash -n` ✅ 通过

---

## 项目文件清理

### 删除文件
- `.DS_Store` — macOS 系统缓存文件
- `cluster/` — 空目录（脚本用的是 `~/cluster/`）
- `riwi.sh.bak_rw_bai_to_rw_lv` — 旧颜色替换备份

---

## 国内服务器 dash→bash 问题

### 原因
国内腾讯云服务器默认 shell 是 dash，不支持 `<( )` 进程替换语法

### 解决
- 方法一：`sudo dpkg-reconfigure dash` → 选 No
- 方法二：`sudo ln -sf /bin/bash /bin/sh`
- 推荐运行命令：`curl -sL URL | bash`（不依赖 /bin/sh）

---

## 主菜单删除 "11. 安全优化"

### 操作内容
- 删除 `linux_security()` 函数（原行21500-21648）及其注释头
- 删除 `user_permissions_menu()` 函数（原行21649-21885），该函数仅被 linux_security 调用
- 删除主菜单 "11. 安全优化" 菜单项
- 删除 case 条目 `11) linux_security ;;`
- 重新编号菜单项：12→11（热门专栏）、13→12（其他管理）
- 共删除390行代码

---

## 合并"系统工具"到"日常维护"

### 操作内容
- 删除主菜单 "9. 系统工具" (linux_Settings 函数)
- 将系统工具的所有功能合并到 "2. 日常维护" (update_clean_menu 函数)
- 主菜单重新编号：10→9, 11→10, 12→11
- 替换所有 linux_Settings 引用为 update_clean_menu（BBRv3、elrepo函数中的返回调用）
- 日常维护状态栏新增"防火墙"状态显示

### 新的日常维护菜单结构（46项）

| 分区 | 编号 | 功能 |
|------|------|------|
| 安全加固 | 1-7 | 修改SSH端口、禁用ROOT、密钥登录、SSH防御、病毒扫描、修复SSH漏洞、隐私安全 |
| 性能优化 | 8-13 | BBR3、内核优化、虚拟内存、一条龙调优、限流关机、红帽内核升级 |
| 监控排查 | 14-17 | 查看端口、网卡管理、系统日志、TG-bot预警 |
| 数据管理 | 18-21 | 文件管理器、rsync同步、备份恢复、定时任务 |
| 网络管理 | 22-25 | 优化DNS、ipv4/ipv6、host解析、防火墙管理器 |
| 用户管理 | 26-28 | 用户管理、密码生成器、ssh远程连接 |
| 系统配置 | 29-33 | 基础设置、时区、主机名、语言、更新源 |
| 高级工具 | 34-39 | 命令行美化、历史记录、收藏夹、变量管理、r命令、回收站 |
| 系统维护 | 40-45 | 硬盘分区、系统更新、系统清理、重装系统、重启、卸载 |

### 语法验证
`bash -n` ✅ 通过

---

## 删除日常维护中的"留言板"

- 删除菜单显示：`──── 其他` 分区标题和 `46 留言板` 菜单项
- 删除 case 条目 `46)`
- 文件中已无"留言板"残留引用
- `bash -n` ✅ 通过

